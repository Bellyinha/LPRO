erros comuns:

- acessar o diretório correto (onde o arquivo package.json está)
- cmd ao invés de powershell
- o correto é "scripts" ao invés de "Scripts"
- auto save "vscode" - nao colocar

desligar o servidor / "limpar" o terminal => ctrl c 2x